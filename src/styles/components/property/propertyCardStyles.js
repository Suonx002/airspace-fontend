import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(theme => ({
    property: {
        padding: '0.75rem'
    },
    propertyImg: {
        width: '100%',
        height: 200,
        objectFit: 'cover',
        objectPosition: 'center'
    }
}))